/*var nodemailer = require('nodemailer');

var transporter = nodemailer.createTransport({
  service: 'gmail',
  host: 'smtp.gmail.com',
  port: 465,
  secure: true,
  auth: {
    user: 'petsbazar.online@gmail.com',
    pass: 'zcusgkcpttxphwbb'
  }
});

var mailOptions = {
  from: 'petsbazar.online@gmail.com',
  to: 'info@petsbazar.online',
  subject: 'Test Email ',
  text: 'Email Received Successfully'
};

transporter.sendMail(mailOptions, function(error, info){
  if (error) {
    console.log(error);
  } else {
    console.log('Email sent: ' + info.response);
  }
});*/